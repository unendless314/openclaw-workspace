---
name: ai-project-architecture
description: Vibe Coding 專用開發流程 - 讓 AI 幫忙設計架構、拆分模組、寫程式碼，PM（人類/AI）只需確認「感覺對不對」。
metadata:
  {
    "openclaw": { "emoji": "⚡" },
  }
---

# AI 專案開發標準作業程序

**核心理念**：人類負責「Vibe Check（感覺對不對）」；AI 負責「架構與實作」。
**適用場景**：中大型專案、Vibe Coding 模式。

## ⚡ 核心四原則

1.  **介面先行（Design First）**：先定義 `interface` 再寫 Code，此為 AI 的紅線。
2.  **文件同步（Doc Sync）**：Code 變了，文件就要變，避免 AI 精神錯亂。
3.  **小步快跑（Iterative）**：一個模組一個 Session，嚴控 Context。
4.  **人機合作（Human/AI Co-op）**：人類確認方向，AI 主導。

---

## 🚀 四階段開發流程

### Phase 1: 藍圖（Architecture）

**目標**：產出模組清單與依賴關係。

```bash
kimi --print -w ~/project -p "分析需求並輸出架構文件 (docs/):
1. architecture.md（架構圖與模組依賴）
2. modules.md（模組職責與開發順序）
3. 預估複雜度與潛在風險"
```

### Phase 2: 契約（Interface & Criteria）

**目標**：定義模組的「輸入輸出」與「驗收標準」。

```bash
kimi --print -w ~/project -p "為 [模組名] 定義介面 (docs/interfaces/):
1. 輸入/輸出型別（Interface/Dataclass）
2. 公開函數簽名（不寫實作）
3. **驗收標準**：產生一份 curl 指令或測試腳本，讓人類能驗證功能是否正常"
```

> 🛑 **Vibe Check**：介面看起來合理嗎？測試腳本你能在本地執行嗎？

### Phase 3: 實作（Implementation Loop）

**目標**：依賴介面實現功能，並保持文件同步。

**循環步驟**：
1.  **Coding**: `kimi --print --yolo -w ~/project -p "實作 [模組名]，嚴格遵守 interface"`
2.  **Sync**: `kimi --print --continue -w ~/project -p "若實作過程有調整介面，請同步更新 docs/interfaces 與 architecture.md"`
3.  **Test**: 執行 Phase 2 產生的驗收腳本。
4.  **Git**: `git add . && git commit -m "feat: [模組名]"`

> ⚠️ **Context 警報**：每個模組完成後，建議 **重啟 Session**。

### Phase 4: 整合（Integration）

**目標**：將模組串接，解決「接口不合」的問題。

```bash
kimi --print -w ~/project -p "執行全系統整合測試，檢查：
1. 模組間介面是否匹配
2. 循環依賴問題
3. 輸出修復計劃"
```

---

## 🛠️ 異常處理（Troubleshooting）

**狀況 A：AI 鬼打牆（連續 3 次修不好 Bug）**
*   **動作**：`git reset --hard` 回到上個版本。
*   **指令**：`kimi --print -w ~/project -p "停止寫 Code。請先閱讀錯誤 log，分析根本原因，給出 3 個可能的解決方案，讓我選擇。"`

**狀況 B：AI 忘記設定或幻覺**
*   **動作**：強制 AI 複習文件。
*   **指令**：`kimi --print -w ~/project -p "請重新閱讀 docs/architecture.md 與 docs/interfaces，確認目前的實作是否符合設計。"`

**狀況 C：功能越改越爛**
*   **動作**：檢查是否 Context 爆了（>70%）。如果是，立刻 Commit 並開新 Session。

---

## 📂 建議結構

```text
project/
├── AGENTS.md
├── docs/
│   ├── PRD.md              # 產品需求（人類和 AI 討論後由 AI 寫）
│   ├── architecture.md     # 唯一真理：架構圖
│   ├── modules.md          # 開發順序表
│   ├── decisions.md        # 重要決策記錄
│   └── interfaces/         # 契約：各模組 *.ts/*.py 定義
│       ├── auth.interface.ts
│       └── ...
├── modules/                # 實作程式碼
├── tests/                  # 驗收腳本 (Generated by AI)
└── README.md
```
